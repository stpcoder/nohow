# 여기만 — 제품 기획서

## 한 문장 정의

사용자의 최근 클릭과 현재 화면을 시스템의 기능 지도에 겹쳐, 하려던 업무와 그 판단 근거를 보여주고 원본 실행은 사람에게 남기는 Chrome Side Panel입니다.

핵심 문장: **여기까지 온 경로가 곧 프롬프트입니다.**

## 문제와 전환

복잡한 사내 시스템은 기능이 부족해서가 아니라 정보 구조가 사용자의 목적과 다르기 때문에 어렵습니다. 기존 PoC처럼 “무엇을 하려고 왔나요?”라고 다시 물으면 사용자는 이미 클릭으로 남긴 맥락을 채팅창에 반복해서 설명해야 합니다. 반대로 에이전트가 사이트를 무작정 탐색하고 실행하면 판단 근거와 책임 경계가 흐려집니다.

여기만은 사용자가 원래 시스템에서 한 행동을 가장 먼저 봅니다.

- 사용자는 평소처럼 원본 메뉴를 누릅니다.
- 여기만은 최근 클릭 5개를 `Intent Trace`로 만듭니다.
- 운영자가 승인한 `Capability Graph`와 교차합니다.
- 같은 업무에서 만난 근거가 3개 이상일 때만 목적을 제안합니다.
- 사용자가 확인하면 필요한 원본 단계만 다시 구성합니다.
- 약관·비용·개인정보·최종 실행은 사용자가 직접 처리합니다.

## 왜 Chrome Side Panel인가

- 원본 웹과 나란히 유지되어 근거와 실제 화면을 동시에 볼 수 있음
- 시스템 API나 프론트엔드를 전면 재개발하지 않고 적용 가능
- `activeTab`으로 사용자가 연 현재 탭에만 권한 부여
- 목적 복원 뒤에도 원본 약관·입력·버튼을 그대로 유지
- 멤버사별 Capability Graph만 교체해 공통 Resolver와 Human Gate 재사용

## 핵심 구성

### Intent Trace

클릭한 요소의 보이는 이름, 페이지 문맥, 시각만 탭별 세션에 최대 5개 저장합니다. 입력창 값과 문서 본문은 읽지 않습니다. 사용자는 언제든 흔적을 지울 수 있습니다.

### Capability Graph

업무 ID, 화면 경로, 선행 조건, 위험도와 원본 요소를 연결한 검수형 기능 지도입니다. AI가 초안을 만들 수 있지만 운영자가 승인한 버전만 배포합니다.

### Evidence Resolver

각 클릭과 업무 노드의 신호가 얼마나 겹치는지 가중 점수로 계산합니다. 현재 PoC는 `서로 다른 근거 3개 + 점수 15`를 모두 만족할 때만 목적을 제안합니다. 확률 숫자 대신 일치한 클릭과 경로를 보여줍니다.

### Human Gate

조회·탐색은 연결할 수 있지만, 탈퇴·삭제·결제·제출·승인처럼 책임이 큰 행동은 원본 위치만 표시합니다. 실제 입력과 클릭은 사용자가 수행합니다.

## 대표 시나리오

1. 사용자가 여기만을 열고 사이트를 읽습니다.
2. 원본 포털에서 `복지 포인트`, `제휴 복지몰`, `회원관리`를 누릅니다.
3. 세 클릭이 `제휴 복지몰 회원 해지` Capability와 만납니다.
4. 여기만이 목적 후보와 세 근거를 함께 보여줍니다.
5. 사용자가 `이 목적이 맞아요`를 누릅니다.
6. 포인트 확인 → 약관 읽기 → 직접 신청의 3단계가 나타납니다.
7. 원본 약관·해지 사유·체크박스·최종 버튼은 사용자가 직접 처리합니다.

## 현재 PoC 구현 범위

- 42개 탐색 메뉴의 가상 레거시 포털
- 실제 DOM 조작 요소를 읽는 안전 탐색
- 클릭 이벤트의 탭별 `chrome.storage.session` 보존
- 3개 업무 노드의 Capability Graph
- 가중 교집합과 3근거 임계값
- 판단 근거와 그래프 경로의 실시간 시각화
- 목적 확인 뒤 동적 3단계 UI
- 원본 약관·체크박스·예약표 스크롤 및 하이라이트
- 위험 행동 자동 클릭 0회
- 실제 설치 가능한 Manifest V3 확장 프로그램
- Playwright 상호작용 영상과 데스크톱·모바일 QA

의도적으로 제외한 범위:

- 실제 사내 URL·화면·데이터
- 실제 탈퇴·결제·삭제 동작
- 승인 없는 자동 클릭 크롤링
- 모델 API 키와 외부 전송
- 아직 연결하지 않은 LLM을 사용한다고 주장하는 표현

## AI의 구체적 역할

런타임에서 매번 모든 것을 LLM에게 묻지 않습니다. AI는 시스템 온보딩의 `Graph Compiler`로 사용합니다.

1. 읽기 전용 수집기가 DOM label, role, route, 폼 경계를 모읍니다.
2. 보안 승인 AI가 메뉴 용어를 공통 업무 ID로 정규화하고 그래프 초안을 만듭니다.
3. 시스템 운영자가 경로·권한·선행 조건·위험도를 검수합니다.
4. 승인된 그래프를 서명·버전 관리해 런타임에 배포합니다.
5. 화면 변경 시 영향받은 노드만 다시 검수합니다.

이 구조는 사용자마다 긴 화면 인벤토리를 모델로 보내는 비용과 지연을 줄이고, 의도 판단을 재현 가능하게 만들며, 전사 공통 Human Gate 정책을 적용할 수 있게 합니다.

## 성공 지표

PoC에서 확인한 사실:

- 탐색 메뉴 42개
- 클릭 근거 3개로 대표 목적 복원
- 회원 탈퇴 3단계 재구성
- 위험 행동 자동 클릭 0회
- 입력값 수집 0건
- 빌드·lint·Intent Engine 테스트·핵심 E2E 통과
- 모바일 가로 오버플로 0

파일럿에서 측정할 항목:

- 과업 성공률과 전체 처리 시간
- 잘못된 메뉴 클릭 수
- 목적 후보 수락률과 수정률
- 헬프데스크 문의·매뉴얼 검색 횟수
- 사용자 통제감·이해도·신뢰도
- 접근성 사용자 과업 성공률

## 3단계 로드맵

### 1단계 — 단일 시스템 검수형 파일럿

회의실 예약, 증명서 발급, 복지몰 회원관리의 작은 Capability Graph를 운영자가 검수하고 10명 안팎의 가상 과업 테스트를 진행합니다.

### 2단계 — 그룹 공통 업무 온톨로지

멤버사마다 다른 메뉴 이름을 공통 업무 ID에 연결하고 그래프 소유자·버전·변경 감지를 운영합니다.

### 3단계 — 접근성 프로필과 멀티모달

큰 글씨, 키보드 전용, 음성 안내, 쉬운 말 프로필을 제공하고 모바일 앱·키오스크·공공서비스로 확장합니다.

## 가장 강한 데모 문장

“사용자는 이미 클릭으로 목적을 말하고 있었습니다. 여기만은 그 경로를 기능 지도에 겹쳐 왜 회원 해지라고 판단했는지 보여주고, 실행 권한은 끝까지 사람에게 남깁니다.”
